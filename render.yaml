services:
  - type: web
    name: dermai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py --dashboard
    envVars:
      - key: PORT
        value: 10000
